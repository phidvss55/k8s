apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-persistent-volume-claim
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: host-persistent-volume
  resources:
    requests:
      storage: 1Gi
